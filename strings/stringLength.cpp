#include <iostream>
#include <string>
using namespace std;

int main() {
    string song = "amar sonar bangla.";

    int len = song.length();
    cout << len << '\n';

    int len2 = song.size();
    cout << len2;

    return 0;
}
